{
  "name": "server",
  "version": "1.0.0",
  "description": "",
  "main": "index.js",
  "scripts": {
    "test": "echo \"Error: no test specified\" && exit 1",
    "start": "SET NODE_OPTIONS=--openssl-legacy-provider && netlify-lambda serve src",
    "build": "SET NODE_OPTIONS=--openssl-legacy-provider && netlify-lambda build src"
  },
  "author": "",
  "license": "ISC",
  "dependencies": {
    "body-parser": "^1.20.2",
    "cors": "^2.8.5",
    "express": "^4.18.2",
    "knex": "^2.4.2",
    "netlify-lambda": "^2.0.16",
    "pg": "^8.10.0",
    "serverless-http": "^3.2.0"
  }
}
